{
  "name": "write-the-realm",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "rm -rf dist && mkdir -p dist/src && esbuild src/main.js --bundle --format=esm --platform=browser --target=es2020 --outfile=dist/src/main.js && cp index.html offline.html service-worker.js dist/ && cp -r assets js dist/"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  },
  "dependencies": {
    "three": "^0.180.0"
  }
}
